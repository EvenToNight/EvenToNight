services:
  interactions:
    deploy:
      replicas: ${INTERACTIONS_SERVICE_REPLICAS:-1}
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
        failure_action: rollback
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.interactions.rule=Host(`interactions.${HOST}`)"
        - "traefik.http.routers.interactions.entrypoints=web"
        - "traefik.http.services.interactions.loadbalancer.server.port=${INTERACTIONS_SERVICE_PORT}"
        - "traefik.docker.network=${COMPOSE_PROJECT_NAME}_eventonight-network"
    depends_on: []

  mongo-interactions:
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.labels.mongo.interactions.1 == true
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 5

networks:
  eventonight-network:
    driver: overlay
    attachable: true
  interactions-network:
    driver: overlay
