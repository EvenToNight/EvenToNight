coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 10%
        base: auto
    patch:
      default:
        enabled: false

comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default
  require_changes: false

github_checks:
  annotations: true